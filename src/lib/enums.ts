export enum EnumGender {
  MALE = "male",
  FEMALE = "female",
}

export enum TaskPriority {
  Low = "Low",
  Medium = "Medium",
  High = "High",
}

export enum TaskStatus {
  Todo = "To do",
  InProgress = "In Progress",
  Completed = "Completed",
  Expired = "Expired",
}
